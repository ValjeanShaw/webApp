<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lucky.mapper.IUserMapper">


    <select id="getUserById" resultType="com.lucky.pojo.User" parameterType="int">
        SELECT *
        FROM user
        WHERE id=#{id}
    </select>

    <insert id=""></insert>


</mapper>