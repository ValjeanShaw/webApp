<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <!-- 别名的定义 第一种方式-->
    <typeAliases>
        <!-- 批量定义别名 ，指定包名，自动扫描包中的类，别名即为类名，首字母小写-->
        <package name="com.lucky.pojo"/>
    </typeAliases>
    <!-- 别名的定义 第二种方式-->
    <!--
    <typeAliases>
        <typeAlias alias="User" type="com.lucky.pojo.User"/>
    </typeAliases>
    -->

</configuration>